{
  "$schema": "https://cursor.com/schemas/hooks.json",
  "hooks": {
    "afterAgentResponse": {
      "command": "~/.response-boxes/hooks/cursor-collector.sh",
      "description": "Collect response boxes from agent responses (observation-only)"
    }
  }
}
